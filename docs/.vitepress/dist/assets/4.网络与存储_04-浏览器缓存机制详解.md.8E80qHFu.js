import{_ as e,c as t,o as c,ae as i}from"./chunks/framework.DK_uz7nx.js";const u=JSON.parse('{"title":"13-浏览器缓存机制详解","description":"","frontmatter":{},"headers":[],"relativePath":"4.网络与存储/04-浏览器缓存机制详解.md","filePath":"4.网络与存储/04-浏览器缓存机制详解.md"}'),r={name:"4.网络与存储/04-浏览器缓存机制详解.md"};function l(d,o,n,a,s,g){return c(),t("div",null,[...o[0]||(o[0]=[i('<h1 id="_13-浏览器缓存机制详解" tabindex="-1">13-浏览器缓存机制详解 <a class="header-anchor" href="#_13-浏览器缓存机制详解" aria-label="Permalink to &quot;13-浏览器缓存机制详解&quot;">​</a></h1><p>浏览器缓存，也称为 HTTP 缓存，是 Web 性能优化的核心策略。它允许浏览器将一度请求过的资源（如图片、CSS、JS 文件）存储在本地，当再次需要该资源时，可以直接从本地存储中读取或通过简单的验证后使用，从而避免了不必要的网络请求。</p><p>浏览器缓存机制主要分为两大类：<strong>强缓存</strong> 和 <strong>协商缓存</strong>。</p><h2 id="_1-强缓存-strong-cache" tabindex="-1">1. 强缓存 (Strong Cache) <a class="header-anchor" href="#_1-强缓存-strong-cache" aria-label="Permalink to &quot;1. 强缓存 (Strong Cache)&quot;">​</a></h2><ul><li><strong>定义</strong>：强缓存是指浏览器在请求资源时，如果发现本地缓存的副本<strong>未过期</strong>，就<strong>不会向服务器发送任何请求</strong>，而是直接从本地缓存中读取该资源。</li><li><strong>状态码</strong>：在这种情况下，网络请求甚至不会发生，因此在 Chrome DevTools 的 Network 面板中，该资源的状态码会显示为 <code>200 OK (from disk cache)</code> 或 <code>200 OK (from memory cache)</code>。</li><li><strong>优点</strong>：速度最快，因为它完全避免了网络往返。</li></ul><p>强缓存是由服务器通过设置响应头中的特定字段来控制的。主要有两个相关的头部，它们可以同时存在，但优先级不同。</p><h3 id="a-expires-http-1-0" tabindex="-1">a. <code>Expires</code> (HTTP/1.0) <a class="header-anchor" href="#a-expires-http-1-0" aria-label="Permalink to &quot;a. `Expires` (HTTP/1.0)&quot;">​</a></h3><ul><li><strong>格式</strong>：<code>Expires: Wed, 22 Oct 2025 08:28:00 GMT</code></li><li><strong>原理</strong>：<code>Expires</code> 的值是一个<strong>绝对时间</strong>的 GMT 格式字符串，它告诉浏览器该资源的缓存副本在此时间点之前都是有效的。</li><li><strong>缺点</strong>：由于它是一个绝对时间，如果客户端的本地时间与服务器时间不一致（例如用户手动修改了本地时间），可能会导致缓存提前失效或永远有效，存在不确定性。</li></ul><h3 id="b-cache-control-http-1-1-优先级更高" tabindex="-1">b. <code>Cache-Control</code> (HTTP/1.1) - 优先级更高 <a class="header-anchor" href="#b-cache-control-http-1-1-优先级更高" aria-label="Permalink to &quot;b. `Cache-Control` (HTTP/1.1) - 优先级更高&quot;">​</a></h3><ul><li><strong>格式</strong>：<code>Cache-Control: max-age=3600</code></li><li><strong>原理</strong>：<code>Cache-Control</code> 是 HTTP/1.1 中引入的，功能更强大。它使用一个<strong>相对时间</strong> <code>max-age</code>（单位为秒）来告诉浏览器，资源在被请求后的 <code>3600</code> 秒内都是有效的。</li><li><strong>优点</strong>：<code>max-age</code> 使用相对时间，不受客户端本地时间的影响，比 <code>Expires</code> 更精确、更可靠。</li><li><strong>其他常用指令</strong>： <ul><li><code>no-cache</code>：<strong>强制进行协商缓存</strong>。浏览器在每次请求前，都必须向服务器发送验证请求，确认资源是否依然有效。</li><li><code>no-store</code>：<strong>完全禁用缓存</strong>。浏览器既不使用本地缓存，也不存储该资源的任何副本。</li><li><code>public</code>：表明该资源可以被任何中间代理（如 CDN、代理服务器）缓存。</li><li><code>private</code>：表明该资源是用户私有的，只能被最终用户的浏览器缓存，不允许中间代理缓存。</li></ul></li></ul><p><strong>当 <code>Cache-Control</code> 和 <code>Expires</code> 同时存在时，<code>Cache-Control</code> 的优先级更高。</strong></p><h3 id="c-强缓存的存储位置-memory-cache-vs-disk-cache" tabindex="-1">c. 强缓存的存储位置：Memory Cache vs Disk Cache <a class="header-anchor" href="#c-强缓存的存储位置-memory-cache-vs-disk-cache" aria-label="Permalink to &quot;c. 强缓存的存储位置：Memory Cache vs Disk Cache&quot;">​</a></h3><p>当强缓存生效时，我们会在 Chrome DevTools 的 Network 面板中看到 <code>(from memory cache)</code> 或 <code>(from disk cache)</code>。这表示缓存资源是存储在<strong>内存</strong>还是<strong>磁盘</strong>中。</p><ul><li><p><strong>Memory Cache (内存缓存)</strong></p><ul><li><strong>优点</strong>：读取速度极快，因为数据直接存储在内存中，没有 I/O 开销。</li><li><strong>缺点</strong>：生命周期短。一旦我们关闭了浏览器标签页，该页面对应的内存缓存就会被清空。</li><li><strong>存储内容</strong>：浏览器会倾向于将体积小、访问频繁的资源（如脚本、字体、小图片）放入内存缓存。</li></ul></li><li><p><strong>Disk Cache (磁盘缓存)</strong></p><ul><li><strong>优点</strong>：容量大，持久性强。即使关闭浏览器甚至重启电脑，磁盘缓存依然存在（只要没过期）。</li><li><strong>缺点</strong>：读取速度比内存缓存慢，需要进行磁盘 I/O 操作。</li><li><strong>存储内容</strong>：体积较大的资源（如大的图片、CSS/JS 文件）会被优先存入磁盘缓存。</li></ul></li></ul><p><strong>读取顺序</strong>：当浏览器请求一个资源时，会<strong>优先在内存缓存中查找</strong>，如果找到则直接使用。如果内存中没有，再去磁盘缓存中查找。如果两者都没有，才会发起网络请求。这种“内存优先”的策略确保了最快的响应速度。</p><h2 id="_2-协商缓存-negotiation-cache" tabindex="-1">2. 协商缓存 (Negotiation Cache) <a class="header-anchor" href="#_2-协商缓存-negotiation-cache" aria-label="Permalink to &quot;2. 协商缓存 (Negotiation Cache)&quot;">​</a></h2><ul><li><strong>定义</strong>：当资源的强缓存失效后（或者 <code>Cache-Control</code> 设置为 <code>no-cache</code>），浏览器在请求该资源时，会<strong>向服务器发送一个验证请求</strong>。服务器会根据请求中携带的缓存标识来判断资源是否有更新。</li><li><strong>工作流程</strong>： <ul><li>如果资源<strong>未更新</strong>，服务器会返回一个 <code>304 Not Modified</code> 的响应，响应体为空。浏览器接收到后，就知道可以使用本地缓存的副本。</li><li>如果资源<strong>已更新</strong>，服务器会返回一个 <code>200 OK</code> 的响应，并在响应体中包含最新的资源内容。</li></ul></li><li><strong>优点</strong>：虽然仍然有一次网络请求，但如果资源未变更，可以避免传输完整的响应体，从而节省网络带宽。</li></ul><p>协商缓存也是通过一对对的请求头和响应头来工作的。</p><h3 id="a-last-modified-if-modified-since" tabindex="-1">a. <code>Last-Modified</code> / <code>If-Modified-Since</code> <a class="header-anchor" href="#a-last-modified-if-modified-since" aria-label="Permalink to &quot;a. `Last-Modified` / `If-Modified-Since`&quot;">​</a></h3><ol><li><strong>首次请求</strong>：服务器在响应头中返回 <code>Last-Modified</code> 字段，表示该资源在服务器上的最后修改时间。 <code>Last-Modified: Tue, 12 Oct 2025 10:00:00 GMT</code></li><li><strong>后续请求</strong>：当缓存过期后，浏览器在请求头中携带 <code>If-Modified-Since</code> 字段，其值就是上次收到的 <code>Last-Modified</code> 的值。 <code>If-Modified-Since: Tue, 12 Oct 2025 10:00:00 GMT</code></li><li><strong>服务器验证</strong>：服务器比较 <code>If-Modified-Since</code> 的值和资源的实际最后修改时间。 <ul><li>如果一致，说明资源未变，返回 <code>304</code>。</li><li>如果不一致，说明资源已更新，返回 <code>200</code> 和新资源，并附带一个新的 <code>Last-Modified</code> 值。</li></ul></li></ol><ul><li><strong>缺点</strong>： <ul><li><strong>精度问题</strong>：时间戳的精度只能到秒，如果在 1 秒内文件被多次修改，服务器无法检测到变化。</li><li><strong>内容未变但时间戳改变</strong>：有时文件内容没有实际变化，但其修改时间却变了（例如重新保存），这会导致缓存失效。</li></ul></li></ul><h3 id="b-etag-if-none-match-优先级更高" tabindex="-1">b. <code>ETag</code> / <code>If-None-Match</code> - 优先级更高 <a class="header-anchor" href="#b-etag-if-none-match-优先级更高" aria-label="Permalink to &quot;b. `ETag` / `If-None-Match` - 优先级更高&quot;">​</a></h3><p>为了解决 <code>Last-Modified</code> 的问题，HTTP/1.1 引入了 <code>ETag</code> (Entity Tag)。</p><ol><li><strong>首次请求</strong>：服务器在响应头中返回 <code>ETag</code> 字段，这是一个根据资源内容生成的<strong>唯一标识符</strong>（类似于文件内容的哈希值）。 <code>ETag: &quot;33a64df551425fcc55e4d42a148795d9&quot;</code></li><li><strong>后续请求</strong>：当缓存过期后，浏览器在请求头中携带 <code>If-None-Match</code> 字段，其值就是上次收到的 <code>ETag</code> 的值。 <code>If-None-Match: &quot;33a64df551425fcc55e4d42a148795d9&quot;</code></li><li><strong>服务器验证</strong>：服务器比较 <code>If-None-Match</code> 的值和资源当前的 <code>ETag</code> 值。 <ul><li>如果一致，说明资源未变，返回 <code>304</code>。</li><li>如果不一致，说明资源已更新，返回 <code>200</code> 和新资源，并附带一个新的 <code>ETag</code> 值。</li></ul></li></ol><ul><li><strong>优点</strong>：<code>ETag</code> 基于内容生成，比基于时间戳的 <code>Last-Modified</code> 更精确、更可靠。</li></ul><p><strong>当 <code>ETag</code> 和 <code>Last-Modified</code> 同时存在时，服务器会优先验证 <code>ETag</code>。</strong></p><h2 id="_3-缓存决策流程总结" tabindex="-1">3. 缓存决策流程总结 <a class="header-anchor" href="#_3-缓存决策流程总结" aria-label="Permalink to &quot;3. 缓存决策流程总结&quot;">​</a></h2><p>当浏览器请求一个资源时，会按照以下流程来决策：</p><ol><li><strong>检查强缓存</strong>：查看本地缓存副本，根据 <code>Cache-Control</code> 和 <code>Expires</code> 判断是否过期。 <ul><li>如果<strong>未过期</strong>，直接使用缓存（<code>200 from cache</code>），流程结束。</li><li>如果<strong>已过期</strong>，进入下一步。</li></ul></li><li><strong>发起协商缓存请求</strong>：向服务器发送请求，并在请求头中携带 <code>If-Modified-Since</code> 和 <code>If-None-Match</code>。</li><li><strong>服务器验证</strong>：服务器根据收到的缓存标识进行判断。 <ul><li>如果资源<strong>未更新</strong>，返回 <code>304</code>。浏览器使用本地缓存，流程结束。</li><li>如果资源<strong>已更新</strong>，返回 <code>200</code> 和最新的资源内容。浏览器使用新资源，并更新本地缓存及其过期信息，流程结束。</li></ul></li></ol><h2 id="_4-用户行为对缓存的影响" tabindex="-1">4. 用户行为对缓存的影响 <a class="header-anchor" href="#_4-用户行为对缓存的影响" aria-label="Permalink to &quot;4. 用户行为对缓存的影响&quot;">​</a></h2><ul><li><strong>地址栏输入 URL / 书签 / 前进后退</strong>：正常检查缓存。</li><li><strong>F5 / 点击刷新按钮</strong>：会跳过<strong>强缓存</strong>，直接发起<strong>协商缓存</strong>请求（请求头会带 <code>If-Modified-Since</code> / <code>If-None-Match</code>）。</li><li><strong>Ctrl + F5 强制刷新</strong>：会<strong>完全忽略</strong>所有缓存，直接向服务器请求全新的资源（请求头不会带任何缓存标识）。</li></ul><h2 id="_5-现代前端工程化缓存策略" tabindex="-1">5. 现代前端工程化缓存策略 <a class="header-anchor" href="#_5-现代前端工程化缓存策略" aria-label="Permalink to &quot;5. 现代前端工程化缓存策略&quot;">​</a></h2><p>理论知识需要结合工程实践才能发挥最大价值。在现代前端开发（如使用 Webpack/Vite）中，我们通常采用一种被称为“<strong>持久化缓存 + 文件指纹</strong>”的最佳实践策略，来最大化地利用缓存并确保内容能被可靠地更新。</p><ul><li><p><strong>核心思想</strong>：</p><ol><li>对静态资源（JS, CSS, 图片等），在文件名中加入根据其内容计算出的 <strong>Hash（文件指纹）</strong>。</li><li>为这些带指纹的静态资源设置一个<strong>超长时间的强缓存</strong>。</li><li>作为入口的 <code>index.html</code> 文件，<strong>绝不设置强缓存</strong>，而是使用协商缓存或禁用缓存。</li></ol></li><li><p><strong>具体做法</strong>：</p><ol><li><strong>文件名 Hash</strong>：构建工具（如 Webpack）在打包时，会根据文件内容计算出一个唯一的哈希值，并将其作为文件名的一部分（例如 <code>app.a1b2c3d4.js</code>）。</li><li><strong>长效强缓存</strong>：服务器对所有带哈希的静态资源文件（如 <code>*.js</code>, <code>*.css</code>）设置一个极长的 <code>Cache-Control</code> 值，例如： <code>Cache-Control: max-age=31536000</code> (一年)</li><li><strong>入口文件控制</strong>：服务器对 <code>index.html</code> 文件设置协商缓存，以确保浏览器每次都能获取到最新的 HTML 内容： <code>Cache-Control: no-cache</code></li></ol></li><li><p><strong>工作流程</strong>：</p><ul><li>当网站内容更新，某个 JS 文件的内容发生变化时，其文件名中的哈希值也会随之改变（例如从 <code>app.a1b2c3d4.js</code> 变为 <code>app.e5f6g7h8.js</code>）。</li><li>由于 <code>index.html</code> 没有被强缓存，浏览器会通过协商缓存获取到最新的 HTML 内容。</li><li>最新的 HTML 中引用了新的 JS 文件名。浏览器在请求这个新文件时，会因为文件名不同而发生一次全新的网络请求，从而加载到最新的资源。</li><li>对于那些内容<strong>未发生变化</strong>的静态资源，它们的文件名和哈希值保持不变，浏览器将继续从本地缓存中以极快的速度（强缓存）加载它们，无需任何网络请求。</li></ul></li><li><p><strong>优势</strong>：这种策略完美地结合了强缓存的“快”和内容更新的“准”，实现了<strong>最大化的缓存利用率</strong>和<strong>可靠、即时的内容更新</strong>，是当前前端工程化领域的黄金标准。</p></li></ul>',34)])])}const f=e(r,[["render",l]]);export{u as __pageData,f as default};

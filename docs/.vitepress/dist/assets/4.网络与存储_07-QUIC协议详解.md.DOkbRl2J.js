import{_ as o,c as r,o as n,ae as a}from"./chunks/framework.DK_uz7nx.js";const h=JSON.parse('{"title":"16-QUIC 协议详解","description":"","frontmatter":{},"headers":[],"relativePath":"4.网络与存储/07-QUIC协议详解.md","filePath":"4.网络与存储/07-QUIC协议详解.md"}'),e={name:"4.网络与存储/07-QUIC协议详解.md"};function i(l,t,s,d,g,c){return n(),r("div",null,[...t[0]||(t[0]=[a('<h1 id="_16-quic-协议详解" tabindex="-1">16-QUIC 协议详解 <a class="header-anchor" href="#_16-quic-协议详解" aria-label="Permalink to &quot;16-QUIC 协议详解&quot;">​</a></h1><p>QUIC (Quick UDP Internet Connections) 是由 Google 设计的一套基于 UDP 的、可靠的、多路复用的、低延迟的安全传输协议。它旨在解决 TCP 协议在现代移动网络环境下的一些固有缺陷，并已成为 HTTP/3 的底层基础。</p><p>可以理解为：<strong>QUIC = TCP + TLS + HTTP/2 的多路复用</strong>。它将 TCP 的可靠传输、TLS 的安全加密以及 HTTP/2 的多路复用能力，在 UDP 之上重新实现了一遍，并进行了深度优化。</p><h2 id="_1-quic-解决了什么核心问题" tabindex="-1">1. QUIC 解决了什么核心问题？ <a class="header-anchor" href="#_1-quic-解决了什么核心问题" aria-label="Permalink to &quot;1. QUIC 解决了什么核心问题？&quot;">​</a></h2><h3 id="a-tcp-的队头阻塞-head-of-line-blocking" tabindex="-1">a. TCP 的队头阻塞 (Head-of-Line Blocking) <a class="header-anchor" href="#a-tcp-的队头阻塞-head-of-line-blocking" aria-label="Permalink to &quot;a. TCP 的队头阻塞 (Head-of-Line Blocking)&quot;">​</a></h3><p>这是 QUIC 要解决的最核心的问题。我们在 HTTP/2 中了解到，虽然它实现了应用层的多路复用，但其底层的 TCP 连接仍然可能发生队头阻塞。如果一个 TCP 数据包丢失，整个连接上的所有 HTTP/2 流都必须暂停，等待该数据包重传成功。</p><p><strong>QUIC 的解决方案</strong>：</p><ul><li>QUIC 基于 UDP，而 UDP 数据包（数据报）之间是相互独立的。</li><li>QUIC 在<strong>自身内部</strong>实现了逻辑上的<strong>流（Stream）</strong>。每个流的数据包都有独立的序列号。</li><li>如果一个流中的某个数据包丢失，QUIC 只会阻塞<strong>这一个流</strong>，而<strong>不会影响</strong>该 QUIC 连接上的任何其他流。这从根本上解决了队头阻塞问题。</li></ul><h3 id="b-连接建立的延迟" tabindex="-1">b. 连接建立的延迟 <a class="header-anchor" href="#b-连接建立的延迟" aria-label="Permalink to &quot;b. 连接建立的延迟&quot;">​</a></h3><ul><li><strong>TCP + TLS 的现状</strong>：一个标准的 HTTPS 连接建立，需要先进行 TCP 的三次握手（通常需要 1.5 RTT，即 1.5 个往返时间），然后再进行 TLS 的握手（也需要 1-2 RTT）。总共需要大约 3 RTT 的延迟才能开始传输应用数据。</li><li><strong>QUIC 的解决方案 (0-RTT &amp; 1-RTT)</strong>： <ul><li>QUIC 将 TCP 握手和 TLS 握手<strong>合并</strong>在了一起。</li><li>对于<strong>首次连接</strong>，客户端和服务器通过 <strong>1 RTT</strong> 就可以完成握手，交换加密所需的所有参数，并建立一个安全的连接。</li><li>对于<strong>再次连接</strong>（客户端已经与该服务器通信过），客户端可以利用之前缓存的服务器配置信息，在发送第一个数据包时就携带上加密的应用数据，从而实现 <strong>0-RTT</strong> 的连接建立。 <ul><li><strong>0-RTT 的安全权衡</strong>：0-RTT 虽然快，但也带来了<strong>重放攻击 (Replay Attack)</strong> 的风险。攻击者可以截获一个 0-RTT 数据包并重新发送给服务器。为了缓解此问题，QUIC 协议要求应用层协议（如 HTTP/3）确保只有<strong>幂等</strong>的请求（如 <code>GET</code>）才能在 0-RTT 数据中发送，对于非幂等操作（如 <code>POST</code> 一个订单）则必须等待 1-RTT 握手完成。</li></ul></li></ul></li></ul><h3 id="c-连接迁移-connection-migration" tabindex="-1">c. 连接迁移 (Connection Migration) <a class="header-anchor" href="#c-连接迁移-connection-migration" aria-label="Permalink to &quot;c. 连接迁移 (Connection Migration)&quot;">​</a></h3><ul><li><strong>TCP 的痛点</strong>：TCP 连接是通过一个四元组来唯一标识的：<code>{源 IP, 源端口, 目标 IP, 目标端口}</code>。当用户的网络环境发生变化时（例如从 Wi-Fi 切换到 4G），客户端的 IP 地址或端口会改变，导致这个四元组失效，TCP 连接必须被中断并重新建立。</li><li><strong>QUIC 的解决方案</strong>： <ul><li>QUIC 连接是通过一个由客户端生成的 64 位<strong>连接 ID (Connection ID)</strong> 来唯一标识的，而不是通过 IP 和端口。</li><li>当网络切换时，即使客户端的 IP 地址变了，只要它能继续使用相同的连接 ID 向服务器发送数据包，服务器就知道这仍然是之前的那个连接，从而可以无缝地迁移，保持通信不中断。这对于移动设备的用户体验提升巨大。</li></ul></li></ul><h2 id="_2-quic-的核心机制" tabindex="-1">2. QUIC 的核心机制 <a class="header-anchor" href="#_2-quic-的核心机制" aria-label="Permalink to &quot;2. QUIC 的核心机制&quot;">​</a></h2><h3 id="a-可插拔的拥塞控制" tabindex="-1">a. 可插拔的拥塞控制 <a class="header-anchor" href="#a-可插拔的拥塞控制" aria-label="Permalink to &quot;a. 可插拔的拥塞控制&quot;">​</a></h3><p>TCP 的拥塞控制算法是硬编码在操作系统内核中的，修改和升级非常困难。QUIC 将拥塞控制的实现放在了<strong>应用层</strong>。</p><p><strong>应用层实现的核心优势</strong>在于<strong>快速迭代与部署</strong>。我们不再需要等待用户更新操作系统内核就能部署最新的网络协议优化，只需要更新浏览器或应用程序即可。这极大地加快了网络技术创新的步伐，允许根据不同的网络环境和应用需求，灵活地切换和优化拥塞控制算法（如 BBR, Reno）。</p><h3 id="b-流量控制" tabindex="-1">b. 流量控制 <a class="header-anchor" href="#b-流量控制" aria-label="Permalink to &quot;b. 流量控制&quot;">​</a></h3><p>QUIC 提供了两种级别的流量控制：</p><ol><li><strong>连接级别 (Connection-level)</strong>：与 TCP 的滑动窗口类似，控制整个连接的总数据发送量。</li><li><strong>流级别 (Stream-level)</strong>：可以为每个独立的流设置流量控制，防止某个流占用过多的缓冲区，影响其他流。</li></ol><h3 id="c-自定义的可靠传输" tabindex="-1">c. 自定义的可靠传输 <a class="header-anchor" href="#c-自定义的可靠传输" aria-label="Permalink to &quot;c. 自定义的可靠传输&quot;">​</a></h3><p>虽然 QUIC 基于不可靠的 UDP，但它在应用层实现了自己的可靠传输机制。</p><ul><li><strong>QUIC Packet</strong>: QUIC 的传输单元是 Packet，每个 Packet 都有一个唯一的序列号。</li><li><strong>ACK 机制</strong>: QUIC 的确认机制（ACK）经过了优化，可以一次性确认多个 Packet，并且明确地报告哪些 Packet 丢失了，从而使重传更加高效。</li></ul><h2 id="_3-quic-的挑战" tabindex="-1">3. QUIC 的挑战 <a class="header-anchor" href="#_3-quic-的挑战" aria-label="Permalink to &quot;3. QUIC 的挑战&quot;">​</a></h2><ul><li><strong>需要服务器和客户端同时支持</strong>：部署 QUIC 需要更新 Web 服务器（如 Nginx, Caddy）和客户端库。</li><li><strong>网络中间设备</strong>：一些旧的网络设备或防火墙可能会因为不认识 UDP 上的特定端口流量而将其丢弃或限速。</li><li><strong>CPU 消耗</strong>：由于 QUIC 的大部分逻辑（加密、拥塞控制等）都在应用层处理，相比于内核实现的 TCP，可能会带来稍高的 CPU 消耗。</li></ul><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><p>QUIC 是对传输层协议的一次现代化重塑。它通过在 UDP 之上构建一个集<strong>安全、可靠、多路复用</strong>于一体的传输层，完美地解决了 TCP 在现代 Web 环境下的诸多痛点，为 HTTP/3 乃至未来的网络应用提供了坚实、高效的基础。</p><table tabindex="0"><thead><tr><th>特性</th><th>TCP + TLS</th><th>QUIC (HTTP/3)</th></tr></thead><tbody><tr><td><strong>队头阻塞</strong></td><td>连接级别阻塞</td><td><strong>流级别阻塞</strong>（核心优势）</td></tr><tr><td><strong>连接建立</strong></td><td>2-3 RTT</td><td><strong>0-1 RTT</strong></td></tr><tr><td><strong>连接标识</strong></td><td>IP + 端口四元组</td><td><strong>连接 ID (Connection ID)</strong></td></tr><tr><td><strong>连接迁移</strong></td><td>不支持，需要重连</td><td><strong>支持</strong></td></tr><tr><td><strong>拥塞控制</strong></td><td>内核实现，更新困难</td><td><strong>应用层实现，可插拔</strong></td></tr><tr><td><strong>多路复用</strong></td><td>无 (由上层 HTTP/2 实现)</td><td><strong>内置</strong></td></tr></tbody></table>',27)])])}const C=o(e,[["render",i]]);export{h as __pageData,C as default};
